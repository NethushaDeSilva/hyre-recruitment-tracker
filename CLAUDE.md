# CLAUDE.md — Hyre

Recruitment and hiring tracker built for a client company called **Altrium**.
Module: COMP50074 · Sprint 2 · Team of 4.

This file is the single source of truth for Sprint 2. Read it in full before doing anything.

---

## 0. How to use this file

- Sections 1–5 are context. Read them, do not skip them — they explain *why* the scope is what it is.
- Section 6 is the build order. Follow it strictly.
- Sections 7–9 are the work.
- Sections 10–13 are non-code items that carry marks.
- Section 14 is how to behave while working.

Rules that apply to every task in this file:

1. **Never build a partial feature.** A half-finished feature scores zero and consumes time the core needed.
2. **Never add a feature not listed here.** Extra scope is what cost marks in Sprint 1.
3. **Before starting any workstream**, state the plan and the files you will change. Wait for confirmation.
4. **If a requirement here is ambiguous, technically wrong, or conflicts with the existing codebase**, say so and stop. Do not guess.

---

## 1. Where these requirements come from

Three sources, in descending order of authority.

**1. The Sprint 1 review lecture (highest authority).**
The module leader spent ~37 minutes reviewing all teams. She defined the recruitment MVP explicitly, named the mistakes she saw, and stated what she marks. She also plays the **Product Owner** role in this project — she said so directly. Her stated process is the requirement.

Caveat: the transcript we have is an automated draft with `[unclear]` gaps and is not certified verbatim. Treat quoted wording as intent, not as exact legal text.

**1b. The 10 September team meeting with her (equal authority, and it SUPERSEDES earlier decisions where they conflict).**
A direct meeting with our team. She reversed the interview-scheduling descope, offered a major simplification, and confirmed several Sprint 1 fixes. Where this meeting contradicts anything derived from the Sprint 1 review lecture, **this meeting wins** — it is later and it is specific to us.

**2. Our team's Sprint 1 viva feedback.**
Specific to us. Two concrete points: candidates should not re-type qualification details that are already in the CV, and interviewer availability is meaningless when tracked only inside our own system.

**3. Altrium's client brief.**
Reported second-hand as: hiring happens across messages and spreadsheets, CVs arrive by email, nobody has a live list of candidates. **This is unverified recollection, not the brief itself.** See section 13.

**Rejected — a fabricated account of Altrium.**
A second classmate produced four "Altrium challenges" (talent drain, giant competition, GenAI skill shortage, role visibility) and described the company as a Sri Lankan engineering boutique. Altrium is fictional and cannot be researched; these were generated by an LLM, not read from the brief. **Do not build to them.** The rubric attached to them rewards a full Kanban lifecycle through Offer and Hired plus department-scoped RBAC, both of which sit past the MVP line in section 3 and would repeat the Sprint 1 mistake.

---

## 2. Stack and conventions

- React + Vite
- Tailwind + shadcn/ui
- Firebase: Auth, Firestore, Storage
- Cloudflare Worker for all AI calls (Workers AI binding)

Hard rules:

- **No AI provider key or token in client code, ever.** All model calls go through the Worker.
- CV files live in Firebase Storage. Extracted structured data lives in Firestore.
- `storage.rules` must be deployed (`firebase.json` needs the `storage` key) and must enforce 5MB and PDF/DOC/DOCX.
- Firestore rules must restrict by role. A candidate must never be able to read the HR pipeline, other candidates, or match scores.
- No component over ~300 lines. Split it.
- No hardcoded or faked AI responses anywhere. The demo runs live.
- Existing candidates have base64 CVs stored in Firestore documents. Those must keep working — do not break backwards compatibility while moving new uploads to Storage.

---

## 3. The MVP bar — this is exactly what gets marked

The module leader defined the recruitment process step by step. **Completing this chain is the MVP.** Her words: *"if you have done up to that point it is a MVP"* and *"that's all you have to do."*

| Step | Requirement |
|---|---|
| 1 | An HR user posts a vacancy |
| 2 | That vacancy is **visible to the public inside our system** — either openly, or behind a candidate self-registration login. She said either is acceptable |
| 3 | A candidate submits a CV against a vacancy. **One person must be able to apply to any number of jobs** |
| 4 | HR ends up with many CVs — realistically 100+ |
| 5 | An **initial filtering mechanism** reduces that to a manageable shortlist |

She named the problem this solves directly: initial filtration is *"one of the main problems we are facing currently with recruitment."*

**Her definition of initial filtration is narrow and specific:**

> *"Initial filtration is a document comparison. What are we comparing? The vacancy with the CV. And we are merely checking what is there in the vacancy — that means the requirements — are they in the CV. That's all."*

That is the product. Not a chatbot. Not natural-language search. A structured comparison of vacancy requirements against CV content, producing a score and a shortlist.

---

## 4. Marking rules that override everything else

These come straight from the lecture. Any decision in this project is settled by checking against them.

**4.1 — At least one stated problem must be genuinely solved.**
> *"The scenario has given exact problems... You need to address at least one. If at least one is not addressed, then your product is not [an MVP]."*

**4.2 — Partial features are worth nothing.**
> *"Do not invest effort if you know that you cannot develop a feature end-to-end."*
Teams that half-built interview scheduling: *"since it is partially developed, it was not considered."* End-to-end means every process and every step in that feature is complete.

**4.3 — A big backlog destroys quality.**
> *"What happens when your sprint backlog is bigger than what you can handle is you do not have time to spend on each feature individually."*
She listed what each feature needs time for: **validity, security, accepting bad data, enough testing, proper design, user friendliness.** That is the per-feature checklist in section 10.

**4.4 — Number of features is our decision, not hers.**
> *"Have I told anybody to develop 7 features, 6 features, 5 features, 4 features?"*
Fewer, complete features beat more, thin ones. This is why the scope below is deliberately small.

**4.5 — It is not a set of features, it is a problem being solved.**
> *"This project is not developing a set of features. It is solving people's problems."*
> *"Everybody has a product. But whether that is the right product to solve the problem is what you need to ask yourself."*

**4.6 — Systems thinking. Ask whether the user can actually do their job.**
> *"Have we given enough data about the [candidate] for someone to assess it? You need to ask yourself."*
For every screen: does the person on this screen have enough information to make the decision this screen exists for?

**4.7 — Development is not where most of the marks were lost.**
> *"Many teams have got lower marks than they thought is because they have not done the rest of it. Not the development."*
Every team had a working product. Documentation, diagrams, backlog, process and demo preparation are where marks were actually lost. See sections 11 and 12.

---

### 4.8 — The test every feature must pass

The client is not buying features. Altrium already has a hiring process; what they do not have is a solution to a problem inside it. A feature that works perfectly but fixes nothing Altrium actually suffers from earns nothing.

Before building anything, and before demoing anything, answer these four in one sentence each. Write the answers down — they go in the sprint document and they are what the viva will probe.

| # | Question |
|---|---|
| 1 | **Which specific Altrium problem does this solve?** Name it, in their words, from their brief. Not "makes hiring easier." |
| 2 | **What does the Altrium employee do today without it?** Describe the current painful workaround — the spreadsheet, the email thread, the manual read of 100 CVs. |
| 3 | **What changes for them after it exists?** Concretely. "HR reads 100 CVs by hand" becomes "HR reviews a ranked shortlist of 12 with visible reasoning." |
| 4 | **Does the person on this screen have enough information to make the decision this screen exists for?** This is her systems-thinking test, stated directly: *"Have we given enough data... for someone to assess it? You need to ask yourself."* |

If question 1 cannot be answered from Altrium's brief, the feature is out of scope regardless of how good it is.

Applied to WS5, our answer is:

1. Altrium's recruiters cannot process the volume of CVs that arrive per vacancy. She named initial filtration as *"one of the main problems we are facing currently with recruitment."*
2. Today a person opens every CV, reads it against the job requirements, and forms a shortlist by hand and by memory. It does not scale and it is inconsistent between reviewers.
3. After: every CV is compared against the vacancy's stated requirements automatically, scored, and ranked, with the matched and missing requirements shown so the recruiter can verify the machine rather than trust it.
4. The shortlist screen shows the score, the requirement-by-requirement breakdown, and one click to the source CV — enough to make a shortlist decision without opening anything else.

**That paragraph is the product.** Everything in Tier 1 exists to make it true. If a task does not serve it, it is not in this sprint.

---

## 5. Known mistakes she named, and whether they apply to us

Audit each of these against the current codebase and report before writing code.

| # | Mistake she named | Applies to Hyre? |
|---|---|---|
| 1 | Vacancy created, but the demonstrator had to **copy-paste the vacancy link into a different app** for anyone to apply — so step 2 (public access) never actually existed | **CHECK — this is WS1** |
| 2 | Using an AI plugin for filtration and **never testing it** | **YES — this is WS6** |
| 3 | Half-building interview assignment/scheduling | **YES — we built it, see WS8** |
| 4 | Not prepared for the demo, no test data, typing `ABCD EFGH` live | **Fix in section 11** |
| 5 | Backlog "all over the place", not matching what was developed | **Team task, section 12** |
| 6 | Diagrams done in isolation from development; generated diagrams the student could not explain | **Team task, section 12** |
| 7 | Daily stand-ups only starting in week 4 or 5 | **Team task, section 12** |

**Confirmed in the 10 September meeting:**

| Point | Her words | Status |
|---|---|---|
| CV re-entry removed | *"Candidates needs to add all data from CV again when applying to the job, so that you need to get rid of"* | Confirmed done (WS2) |
| Initial filtering is **inaccurate** | *"Initial filtering feature is inaccurate, right. That needs to be looked at."* | WS5 rebuild is the fix |
| Candidate comparison | *"Comparing candidate also not needed."* | Confirmed removed |
| Scheduling | *"you need to fix the schedule interview feature"* | **Reinstated — WS8** |
| End-to-end, defined | *"during the demo all your features had gaps in them... without fine tuning and completing a function you have gone to other functions"* | Each feature complete, no gaps |

Our own viva added:

| # | Our specific feedback |
|---|---|
| 8 | Candidates were made to re-type degree/experience into form fields when the CV already contains it — **fixed in WS2** |
| 9 | A random non-CV document uploaded successfully — **WS3** |
| 10 | Interviewer availability tracked only inside Hyre, ignoring their real job — **WS8** |
| 11 | Side-by-side candidate comparison judged unnecessary — **remove it** |

---

## 6. Build order

Strict. Do not start a tier until the tier above is complete, tested, and committed.

**TIER 0 — Unblock**
- Fix the Cloudflare Worker connection. Nothing downstream works while it is unreachable.

**TIER 1 — The MVP chain (non-negotiable)**
- WS1 Public vacancy access
- WS2 Frictionless application *(built — verify only)*
- WS3 CV validation gate *(built — Worker broken)*
- WS4 CV parsing and extraction
- WS5 Initial filtration
- WS6 Calibration and test evidence

- WS8 Interview scheduling and interviewer availability — **REINSTATED 10 Sep.** See section 9.

**CUT — do not build**
- WS7 Cross-vacancy live candidate board. The shortlist screen HR needs lives inside WS5.

**REMOVE**
- Side-by-side candidate comparison. Delete the component, the route, and the nav entry.

---

## 7. Tier 0 — fix the Worker

The validation UI currently shows *"We couldn't check your CV right now — this is on our end, not your file."* That error state is behaving correctly; the Worker is not reachable.

Diagnose in this order and **report findings before changing code**:

1. Log the exact URL being called at runtime, immediately before the `fetch`. **If it contains `undefined`, the env var is not reaching the build** — confirm it is `VITE_` prefixed, present in `.env` at project root, and that the dev server was restarted after adding it. Vite does not hot-reload `.env`.
2. Confirm the Worker is actually deployed (`wrangler deploy` run, not just a local file). Show the wrangler config and the route.
3. CORS — does the Worker return `Access-Control-Allow-Origin` for the dev origin, and does it handle the `OPTIONS` preflight? A JSON `POST` will preflight.
4. Log the real HTTP status code and raw response body in the catch block, not a generic message.

Then fix the root cause and add permanently:
- Real status and Worker error logged to console on failure. User-facing message stays friendly.
- A health-check call to the Worker on app load, so misconfiguration surfaces immediately instead of when a candidate applies.
- One automatic retry with short backoff before showing the error state.

**Never add a fallback that lets submission through when the Worker is unreachable.** Blocked is correct.

---

## 8. Tier 1 workstreams

### WS1 — Public vacancy access

**She flagged this exact failure:** teams created a vacancy, then *"they need to copy paste a link of the vacancy in order for people to submit CVs"* — pasting it into a different app during the demo. Her point: step 2 of the process, the public being able to access vacancies, did not exist.

**Audit first.** In a private browser window with no prior link, can a visitor find and open an open vacancy? Report yes or no before changing anything.

Requirements:

- A public vacancy listing route inside the app, reachable from the landing page. No copy-pasted links, no external tools.
- The listing shows: title, department, location, employment type, posted date, short summary. Search and filter by department and location.
- A vacancy detail page with full description, requirements, and an Apply action.
- Pick **one** access model and implement it completely:
  - **Open** — anyone can browse and apply; identity is the email address, or
  - **Candidate portal** — candidates register and log in, then browse and apply.
  She explicitly said either is acceptable. Do not half-build both.
- **One candidate can apply to many vacancies.** Candidate identity is keyed on email. Applications are separate records referencing one candidate. Their CV and parsed profile persist across applications.
- Applying twice to the same vacancy is prevented with a clear message, not a duplicate record.
- A candidate can view the vacancies they applied to and the status of each.
- Only vacancies with status `open` appear publicly. `draft` and `closed` never leak.

Data shape (adapt to the existing schema, reconcile rather than duplicate):

```
vacancies/{vacancyId}
  title, department, location, employmentType
  description
  requirements: {
    requiredSkills: string[]
    minYearsExperience: number
    requiredQualifications: string[]
    niceToHave: string[]
  }
  status: 'draft' | 'open' | 'closed'
  postedAt, closedAt, createdBy

candidates/{candidateId}          // one per person, keyed on email
  email, phone
  profile: { ...parsed CV fields, see WS4 }
  cvStoragePath, cvUploadedAt
  createdAt

applications/{applicationId}
  candidateId, vacancyId
  status: 'submitted' | 'needs_review' | 'shortlisted' | 'rejected'
  matchScore, matchBreakdown          // from WS5
  appliedAt
```

Acceptance test: private window, no link given. Find a vacancy, apply. Then apply to a second vacancy with the same email. Two applications exist, one candidate record, CV not re-uploaded.

---

### WS2 — Frictionless application flow

*Status: implemented. Verify, do not rebuild.*

The form collects exactly three things: **email, phone, CV upload.** No degree dropdown, no years-of-experience field, no skills checkboxes. Our viva feedback was explicit that making candidates re-type what is already in the CV is the wrong design.

Verification checklist:
- Only those three inputs exist. No dead fields left in the DOM.
- Email and phone format validated client-side, lightly.
- PDF/DOC/DOCX only, 5MB max, enforced client-side and in deployed `storage.rules`.
- Display-name fallback to the email local-part where `fullName` is empty, until WS4 populates it. Display layer only, not a stored field.
- Existing base64-CV candidates still open and download correctly.
- No orphaned files in Storage from rejected uploads (WS3 validates before upload).

---

### WS3 — CV validation gate

*Status: built, blocked on the Worker.*

**She named this test explicitly:** filtration must be tested *"with the random [document] which is not a [CV]... it should ideally identify that, say blank, it should throw an error."*

Flow:

1. Candidate selects a file. **Validation starts immediately** — not on submit.
2. Extract text client-side: `pdfjs-dist` for PDF, `mammoth` for DOCX.
3. Cheap heuristics before any AI call:
   - Text length between 200 and 20,000 characters.
   - Extractable text present. Near-zero text from a PDF means image-only — reject with a message asking for a text-based PDF.
   - CV section markers present (experience, education, skills, work history, contact details). Record which were found.
4. Only if heuristics pass, POST the extracted text to the Worker. Send text, never the file.
5. Worker returns strict JSON:

```json
{ "isCv": true, "confidence": 0.94, "reason": "...", "missingSections": [] }
```

Small models break JSON often. Enforce a response schema, parse defensively, retry once, and if it still fails, fall back to the heuristic result rather than blocking a valid candidate.

6. Decision:
   - **≥ 0.8** → pass. Upload to Storage, enable submit.
   - **0.5–0.8** → pass, but mark the application `needs_review`.
   - **< 0.5 or `isCv` false** → **block**. No candidate record, no upload, submit stays disabled.

7. Upload to Storage happens **only after a pass**, so rejected files are never stored.

UI states on the upload field:

| State | Appearance | Submit button |
|---|---|---|
| Idle | Default dropzone | Disabled |
| Scanning | Animated sweeping gradient border, "Scanning your CV…" | Disabled |
| Passed | Solid success border, filename, confirmation line | **Enabled** |
| Failed | Solid error border, list of what was missing from `missingSections`, one line on what to do | Disabled |
| Error | Distinct state: "We couldn't check your CV right now" + Try again | Disabled |

- Submit is enabled **only** in Passed. Disabled must be visually obvious.
- Replacing the file resets to Scanning and re-runs.
- A Worker outage is the Error state with retry — **never** a rejection message, and **never** a silent pass.
- The scanning animation runs from file selection through text extraction and the network call, not just the network call.
- `prefers-reduced-motion`: static border, no sweep.
- `aria-live` on the status text, `aria-busy` on the field while scanning.
- Log every rejection to `validation_logs` with reason and heuristic results. This is demo evidence.

---

### WS4 — CV parsing and extraction

Once a CV passes validation, the system reads it and fills the candidate profile. The candidate never types this.

Target schema:

```json
{
  "fullName": "string",
  "email": "string",
  "phone": "string",
  "location": "string | null",
  "education": [
    { "degree": "string", "institution": "string", "year": "string | null" }
  ],
  "experience": [
    { "title": "string", "company": "string",
      "startDate": "string | null", "endDate": "string | null",
      "summary": "string" }
  ],
  "totalYearsExperience": 0,
  "skills": ["string"],
  "certifications": ["string"],
  "languages": ["string"]
}
```

Rules:

- JSON only, no prose, no markdown fences. Parse defensively, retry once.
- **Never invent data.** Missing field → `null` or `[]`. A hallucinated qualification is a hiring liability and is treated as a bug, not a rounding error.
- Store the full extracted text alongside the structured data — WS5 needs it.
- If the CV's email differs from the typed email, keep the typed one as primary, store the other as `emailFromCv`, flag the mismatch.
- Confirmation screen after parsing: *"Here's what we read from your CV."* The candidate can correct errors. This is a review of finished work, **not a form to fill in** — do not reintroduce the typing burden WS2 removed.
- Status per candidate: `pending`, `parsing`, `parsed`, `failed`. A `failed` parse is visible to HR, not silently swallowed.
- **Do not let the model compute `totalYearsExperience`.** Extract date ranges only; JavaScript computes the total. See 5.10.
- Process queued, one at a time. Show progress.

---

### WS5 — Initial filtration engine

**This is the core of the product and the thing being marked.**

Her definition: *"Initial filtration is a document comparison. What are we comparing? The vacancy with the CV. And we are merely checking what is there in the vacancy — that means the requirements — are they in the CV."*

#### 5.1 Architecture — hybrid, not pure LLM

The LLM extracts. JavaScript scores. This is not a style preference: a score generated inside an LLM response is opaque and varies between runs, and both properties fail her requirement to demonstrate stable, explainable score ranges.

```
[CV text] ──> [LLM extraction] ──> [structured CandidateProfile] ─┐
                                                                  ├──> [deterministic
[Vacancy] ─────────────────────> [structured Requirements] ──────┘      JS engine]
                                                                              │
                                                        score + per-requirement breakdown
```

Rules:
- **No scoring logic inside any prompt.** The model returns facts, never judgements.
- **Nothing in the scoring path may call an LLM at runtime.** Scoring-stage variance must be provably zero.
- Scoring runs on the parsed object only, never on raw CV text — Worker CPU time is limited and text scanning would consume it.

#### 5.2 Vacancy requirements must be structured

Prerequisite for everything below. If the vacancy form does not capture these, add them first:

```
requirements: {
  requiredQualification: { level: 6|7|8, field: string | null } | null,   // NULLABLE
  requiredSkills: string[],          // mandatory, non-empty
  minYearsExperience: number,
  niceToHave: string[]
}
```

Degree levels: 6 = BSc/BEng/BA, 7 = MSc/MEng, 8 = PhD. `field: null` means any discipline.

**`requiredQualification` is nullable, deliberately.** Forcing every position onto the 6/7/8 scale would make a Diploma or A-Level role overstate its real minimum as a degree — fabricated data, and indefensible if questioned. A position publishes on `requiredSkills` plus `minYearsExperience` alone. Sub-degree minimums stay in the existing free-text `minQualification` / EligibilityTag hint, which is unchanged and unrelated to scoring.

**The publish gate is `requiredSkills`, not qualifications.** A position cannot reach `status: 'open'` with empty `requiredSkills` — enforced at the data layer in `assertPublishableRequirements()`, not only on the form.

**Requirements are editable after creation, not set once.** Editing them **invalidates every score computed under the old requirements**: those scores are marked `stale`, never silently left current and never deleted. The re-score endpoint clears staleness; the edit itself does not.

#### 5.3 Scoring model — 100 points

| Component | Points |
|---|---|
| Mandatory qualifications | 25 |
| Core required skills | 45 |
| Years of experience | 20 |
| Nice-to-have skills | 10 |

**Every number the engine outputs must be derivable from a formula stated here.** If a score cannot be reproduced by hand from these rules, that is a bug.

**Experience** — non-linear, so a large deficit is penalised harder than a small one:

```js
function experienceScore(candidateYrs, requiredYrs) {
  if (requiredYrs === 0) return 20;
  if (candidateYrs >= requiredYrs) return 20;
  return Math.round(20 * Math.pow(candidateYrs / requiredYrs, 1.5));
}
```

Note this returns a flat 20 for anyone at or above the requirement — 3 years and 15 years score identically. That is deliberate: the vacancy stated a minimum, not a preference.

**Skills** — state the exact per-skill credit rule in code and keep it the only place it is defined. Exact match earns full credit; an embedding match at or above threshold earns full credit; below threshold earns nothing. Skills score is the proportion of required skills matched, times 45.

**Degree-level deficit cap** — scaled, never clamped:

```js
capped = Math.round(40 * (raw / 100));
```

A hard `Math.min(raw, 40)` would flatten every affected candidate to exactly 40 and destroy rank order among them. Scaling preserves it: raw 90 → 36, raw 50 → 20.

**Only a degree-level deficit triggers the cap.** A field-of-study mismatch reduces qualification points proportionally and never caps. There is no hardcoded list of acceptable degree fields — an arbitrary whitelist would auto-penalise a Mathematics or Physics graduate moving into software, which is not defensible.

**When `requiredQualification` is null**, the 25 qualification points have no basis and must not be silently deducted from every candidate. Score out of the remaining 75 and normalise:

```js
overallScore = Math.round(((skillsScore + experienceScore + niceToHaveScore) / 75) * 100);
```

Chosen over redistributing the 25 points proportionally across skills and experience: redistribution produces recurring decimals (a 62.3-point skills ceiling), which fights the derivable-by-hand requirement directly. Normalising keeps every sub-component formula byte-identical between the qualified and unqualified cases — only the final aggregation branches. `capApplied` is always `false` here; there is nothing to cap.

In the 5.7 output, `breakdown.qualifications` is `{ "applicable": false }` — never `{ score: 0, max: 0 }`, which would read as a candidate scoring zero rather than a criterion not applying.

#### 5.4 Equivalence matching

Three layers, in order. Only genuine cross-vocabulary content ever reaches layer 3 — the first two resolve everything else before scoring runs.

**Layer 1 — normalisation.** Pure JS, deterministic, no LLM: case, punctuation, `.js`/suffix stripping, trailing version numbers (`Python 3` → `Python`). Runs at scoring time, on both sides, for free.

**Layer 2 — canonicalisation at extraction.** The extraction model (WS4) expands abbreviations and aliases to one canonical form as it extracts — `K8s` → `Kubernetes`, `MBA` → `Business Administration` (level 7). This is fact-reporting, not judgement (5.1 is unaffected: nothing in the scoring path calls an LLM), the same way normalising `Node.js`/`NodeJS` already is. **It must run on both sides** — CV extraction and vacancy `requiredSkills` at save time — or the exact-match check below silently fails whenever HR types the abbreviation a candidate's CV was already expanded past. Existing positions need re-canonicalising retroactively when this ships; treat that as a one-off migration, not an ongoing job. Layer 2 is **not a runtime step** — by the time scoring runs, both sides are already canonical strings, so what layer 1 checks as an "exact match" already *is* the layer-2 match. There are only two cases at runtime: normalise-and-exact-match, or fall through to layer 3.

**Layer 3 — embedding equivalence.** `@cf/baai/bge-base-en-v1.5` cosine similarity, only for terms that survive layers 1 and 2 without matching. Two independent thresholds, both **derived by calibration in WS6, never chosen**: `SKILL_SIMILARITY_THRESHOLD` and `QUAL_SIMILARITY_THRESHOLD` (qualification *field* only — degree *level* stays the strict structural rule, candidate level ≥ required level, and is never part of this comparison). If the vacancy states no required field, skip field comparison entirely.

This layer is deliberately asked to cover two different relations (decision recorded here, not an accident of how pairs were written):
- **equivalence** — genuine synonym, same abstraction level ("Client-side" / "Frontend").
- **category_instance** — a general capability matched by a specific named technology ("Container orchestration" / "Kubernetes"). This relation is **directional in reality and cosine similarity cannot enforce that direction** — a vacancy asking for the general capability, matched by a CV naming the specific tool, is correct; the reverse (vacancy names one specific tool, CV shows only a different tool in the same category) is not, and nothing here tells them apart. Stated as a measured limitation, not silently assumed away.

`SKILL_SIMILARITY_THRESHOLD` is set by exactly one measurement: the **PRIMARY** calibration — keyword vs keyword, short terms both sides, matching the real `skills[]` shape (WS4's schema is a flat array of short terms, never sentences). A **SECONDARY** measurement (phrase vs keyword, the category_instance relation written the way a vacancy might actually phrase a requirement) is run and reported alongside it for documentation of the directionality limitation above — it never feeds the constant. Mixing the two in one calibration run produces a fair-sounding but meaningless number, because a multi-word phrase and a single technical term are structurally different inputs to a sentence embedder regardless of true relatedness (WS6.1).

- **No synonym dictionary.** Hand-written lists cover only the cases the author thought of, which is fabricated coverage.
- **No LLM escalation for borderline pairs.** It would reintroduce runtime variance exactly where decisions are closest. Choose a conservative threshold and accept measured false negatives — a missed synonym is recoverable by a human reviewer, an unstable score is not.
- **Credit rule is strictly above threshold, never at-or-above.** The threshold is defined as `max(hardNegatives)` whenever the distributions overlap (the realistic case, 6.1) — an at-or-above rule would let that exact hard negative satisfy its own boundary, a false positive by construction. Strictly-above closes it without an arbitrary epsilon constant.

**The measured finding (WS6.1, two calibration rounds, run against the corrected, uncontaminated pair sets):** for short technical and academic terms, `bge-base-en-v1.5` cannot reliably separate semantic relatedness from lexical similarity. Pairs that are textually near-identical but semantically distinct (`Angular`/`AngularJS`, `Electrical Engineering`/`Electronic Engineering`) score *higher* than pairs that are semantically equivalent but textually unrelated. Any threshold that correctly excludes the former — which it must, or the engine is not defensible — necessarily excludes nearly all of the latter. Measured false-negative rate on true_match at the calibrated threshold: **100% for skills (PRIMARY), 100% for qualifications.** This is not a broken calibration; two rounds of correcting real methodological errors (contaminated hard negatives, a phrase-vs-keyword genre mismatch) converged on the same result, so it is reported as the finding, not tuned further.

**Consequence, stated plainly: layer 1 (normalisation) plus layer 2 (canonicalisation) do nearly all the real matching work. Layer 3 is a rarely-firing fallback that adds little in practice for short single/multi-word technical terms**, and its main documented value is covering the rare genuine cross-vocabulary case layers 1–2 can't, plus giving the category_instance relation *some* signal even though direction can't be enforced. Do not re-tune the threshold to make this number look better — see WS6.1 for why that would be the wrong response to what was actually measured.

**Open decision, deliberately undecided: keep layer 3, or remove it and run on layers 1–2 alone.** The case for removing it: the calibration evidence above, plus 6.4's finding that the embedding call is not run-to-run reproducible — layer 3 is a measured source of score/rank instability contributing an unknown amount of value in return, since it fires so rarely. Fully deterministic normalisation-plus-exact-match is simpler, faster, cheaper, and cannot be unstable. The case for keeping it: it is the only mechanism that catches a genuine cross-vocabulary match layers 1–2 were never designed for, and removing it forecloses that case entirely rather than accepting it as a documented limitation. **This will be decided from measurement, not preference**: `engine.js` logs, per scored application, how many skill/qualification matches came from each layer (normalisation/exact, embedding, no match) and a BORDERLINE count (matches whose similarity sits within a small band of the threshold — the ones exposed to the 6.4 non-reproducibility finding). Once WS6.3 fixtures run against the engine, the embedding layer's real firing rate and BORDERLINE count on real CVs settle this: near-zero firing means remove it, a non-trivial BORDERLINE count means the instability is real and something has to give. Not yet measured — the instrumentation exists, the fixtures that would exercise it do not yet.

**Thresholds must be uncalibrated until measured.** Ship them so the engine refuses to run rather than silently using a placeholder:

```ts
let calibrated: SimilarityThresholds | null = null;

export function getThresholds(): SimilarityThresholds {
  if (!calibrated) {
    throw new Error(
      "FATAL: similarity thresholds are UNCALIBRATED. " +
      "Run 'npm run calibrate' before executing the scoring engine."
    );
  }
  return calibrated;
}
```

Never commit a numeric literal for either threshold — `thresholds.js` loads them from the calibration script's generated output, never a hand-typed constant.

#### 5.5 Embedding economics

- Embed each unique string once per request, then compute cosine locally. 20 CV skills against 8 requirements is 28 embeddings, not 160 comparisons.
- Cache vacancy-requirement embeddings — they are static.
- Free tier is 10,000 Neurons/day. Track consumption and never run the full test suite on demo day.

#### 5.6 Hallucination control — three outcomes, not two

Binary verify-or-reject deletes correct inferences. "Built REST APIs in Express" legitimately implies Node.js, which no exact match will find.

| Status | Meaning | Credit |
|---|---|---|
| `verified` | Term appears in the CV, word-boundary match after normalisation | Full |
| `inferred` | Not literally present but semantically supported by nearby text, using embeddings already computed | Reduced |
| `unverifiable` | No supporting text anywhere | Dropped, logged |

`inferred` reuses the same embedding call and inherits the same measured limitation as 5.4/6.1: cosine similarity over short spans cannot reliably separate genuine support from lexical coincidence. Treat `inferred` as a low-confidence signal worth reduced credit, never as strong as `verified` — which is already exactly what the credit table does, but it's worth knowing the reduced weighting isn't just caution, it's calibrated caution.

Verification rules:
- **Word-boundary matching, never `.includes()`.** Substring matching passes "R" against any CV containing the letter r, and "Go" against "goals" and "Google". The fabricated entries most likely to survive naive verification are exactly the short ones.
- **Never verify a multi-word term by scattered single words.** "Machine Learning" must not pass against "learning to use the machine".
- Normalise punctuation variants (`Node.js` / `NodeJS` / `node js`) rather than loosening the match.
- Handle symbol-heavy names explicitly: `C`, `R`, `Go`, `C++`, `C#`, `.NET`, `F#`.
- Return structured results with provenance:

```
{ term, status, evidence: string | null, offset: number | null, confidence }
```

- **Return rejections, do not filter them away.** The unverifiable log is the evidence that the AI was tested.
- An invented **qualification** is more serious than an invented skill. Treat an unverifiable qualification as an extraction failure requiring review, not a silently dropped field.

#### 5.7 Output schema

```json
{
  "overallScore": 72,
  "capApplied": false,
  "breakdown": {
    "qualifications": { "score": 25, "max": 25, "levelMet": true,
                        "fieldSimilarity": 0.91, "matched": ["BSc Computer Science"] },
    "coreSkills":     { "score": 30, "max": 45,
                        "matched": [ { "required": "React", "found": "React.js",
                                       "status": "verified", "similarity": 1.0,
                                       "evidence": "...", "offset": 412 } ],
                        "missing": ["Docker"] },
    "experience":     { "score": 13, "max": 20, "candidateYears": 3, "requiredYears": 4 },
    "preferredSkills":{ "score": 0, "max": 10, "matched": [], "missing": ["GraphQL"] }
  },
  "meta": {
    "extractionModel": "...", "embeddingModel": "...",
    "skillThreshold": 0.00, "qualThreshold": 0.00,
    "engineVersion": "1.0.0", "scoredAt": "..."
  }
}
```

Deliberately absent:
- **No `isQualified` boolean.** A verdict on a person is HR's decision via the shortlist threshold, not the engine's.
- **No prose `auditLog`.** LLM-generated prose in a deterministic output reintroduces variance. Generate any summary in code from the breakdown.
- **No numeric score on failure.** A failure has no score at all. Zero is a judgement; a failure is not.

`meta` is not optional — without it, two runs cannot be shown to be comparable, and WS6's whole argument depends on that.

#### 5.8 Ranking and ties

Sort order, deterministic at every level:

1. `overallScore` descending
2. Core skills score descending
3. Candidate ID, lexicographic ascending

ID rather than submission timestamp: FIFO is deterministic but systematically rewards applying early, independent of merit, and that is not defensible if she asks.

Where candidates tie completely, **display them as tied** rather than inventing a hierarchy.

HR sees applications for a vacancy ranked by score, with a threshold control cutting 100 CVs to a workable shortlist. That reduction is the entire point of the product. Every score opens to its breakdown and the source CV — a recruiter must be able to check the machine, not trust it.

#### 5.9 Document handling before the Worker

Client-side heuristics run first, at zero inference cost. Bands are exhaustive and non-overlapping:

| Band | Condition | Action |
|---|---|---|
| **Red** | < 150 chars, or binary corruption markers | Reject locally, 0 Neurons |
| **Yellow** | 150–500 chars, or > 15,000 chars, or missing section markers | **Escalate** to the Worker — the model decides |
| **Green** | 501–15,000 chars with ≥ 2 section markers | Straight to the Worker |

Heuristics produce a **confidence band, not a verdict**. Borderline documents escalate; they are never blocked locally. In particular, do not require an email pattern — candidates now enter their email in the form, and extraction routinely drops header contact details.

**Long documents are truncated, never rejected.** Content-aware, to a 15,000-character budget:

1. Detect sections by heading regex.
2. Retain in full: Summary, Skills, Experience, Education.
3. Truncate next: Projects, Certifications.
4. Prune first: Publications, References, Hobbies.
5. **Fallback when no sections are detected** (designer CVs, single-block layouts, non-English headings): retain the first 7,500 and last 7,500 characters, excise the middle. Never a blind head-slice — on an academic CV that discards the education and skills sections entirely.

Record what happened and surface it:

```json
{ "truncationApplied": true, "truncationStrategy": "SECTION_PRIORITY | HEAD_TAIL_FALLBACK",
  "sectionsPruned": ["Publications"] }
```

**Non-English CVs are not rejected.** We are in Sri Lanka; mixed-language and Sinhala/Tamil sections are normal. Attempt extraction and flag lower confidence. Blanket rejection is a fairness exposure that would have to be defended at a viva.

#### 5.10 Validation and extraction in one call

One combined inference: validate it is a CV *and* extract entities. Splitting them means paying the input-token cost of the full CV twice for the ~95% of uploads that are valid. Junk is caught cheaply by the client-side heuristics before any inference happens, and by `isCV: false` short-circuiting the combined call for whatever slips through.

**Do not let the LLM compute `totalYearsExperience`.** It extracts date ranges; JavaScript computes the total, handling overlapping roles, gaps, "2019 – present", and internships. Arithmetic over dates is exactly where extraction models fail quietly.

**Defensive JSON parsing.** Small models break JSON constantly:
- Strip markdown fences explicitly first.
- Find the first *balanced* object by brace-depth counting. A greedy `/\{[\s\S]*\}/` matches from the first `{` to the **last** `}` in the response and will merge two objects, or swallow trailing prose, into garbage.
- Validate against the expected schema after parsing, not merely that it is syntactically valid JSON.
- Return a discriminated result rather than throwing — the spec requires falling back to the heuristic verdict rather than blocking a valid candidate.
- Handle: output truncated by a token limit, trailing commas, single quotes, and the model returning an explanation instead of JSON.

---

### WS6 — Calibration and test evidence

**A graded deliverable, not polish.** Her position:

> *"Just because it is an AI plugin and it is freely available, you cannot add that to your product and call it a day... you need to test it. Otherwise how can you ensure the quality of your product?"*

#### 6.1 Threshold calibration — run this before anything is scored

This section is scoped to **layer 3 only** (5.4). Abbreviations, aliases and formatting/suffix variants are layer 1/2 concerns — resolved by normalisation and canonicalisation at extraction, never by embeddings — and are tested separately, by construction, not by a similarity threshold (`test-fixtures/calibration-pairs/normalization-canonicalization-pairs.json`).

**Skills — two measurements, only one of which sets the constant.**

- **PRIMARY** (`skills-pairs.json`) — keyword vs keyword, short terms both sides, matching the real `skills[]` shape. 45 pairs, three categories of 15. This alone derives `SKILL_SIMILARITY_THRESHOLD`.
  - True matches (mix of `equivalence` and `category_instance`, per 5.4): `Container orchestration`/`Kubernetes`, `Client-side`/`Frontend`
  - True non-matches: `Python`/`Java`, `Docker`/`Figma`, `SQL`/`HTML`
  - Hard negatives: `Java`/`JavaScript`, `C`/`C++`, `Angular`/`AngularJS`
- **SECONDARY** (`skills-secondary-pairs.json`) — phrase (general capability) vs keyword (named tool), the category_instance relation written the way a vacancy might actually phrase a requirement. Reported alongside PRIMARY, scored against PRIMARY's threshold for illustration only. **Never derives a threshold of its own and never feeds the runtime constant.** Its purpose is solely to document the directionality limitation named in 5.4.

**Qualifications** (`qualifications-pairs.json`). 45 pairs, field-of-study text only — never full degree titles. Degree type/level is resolved separately (5.4 layer 2 / the structural 6-7-8 rule) before this comparison ever runs; feeding full titles in tests type-equivalence, not field-equivalence, and was the root cause of the first failed round below.
- True matches: `Computer Science`/`Software Engineering`, `Data Science`/`Data Analytics`
- True non-matches: `Computer Science`/`Fine Arts`
- Hard negatives: `Computer Science`/`Mathematics`, `Information Technology`/`Business Information Systems`, `Electrical Engineering`/`Electronic Engineering`

**No pair may appear in two categories, in either direction.** A contradictory label (the same two terms marked both true_match and hard_negative) sets the threshold above a true match by construction and guarantees false negatives that are an artefact of labelling, not of the model — audit for this explicitly before running, every time the pair sets change.

**Selection rule.** Run every pair through the embedding model, tabulate the distributions, then:

- If `min(trueMatches) > max(hardNegatives)`, threshold = midpoint of the two.
- **If they overlap** — the realistic outcome — set the threshold at `max(hardNegatives)`. Credit is **strictly above** that value, never at-or-above (5.4) — this keeps the hard-negative false-positive rate at zero without the exact hard negative that defines the threshold trivially satisfying its own boundary. Record the resulting false-negative rate as a measured limitation. Do not escalate to an LLM to resolve the ambiguous zone; that would break scoring determinism.

Whichever single pair sets `max(hardNegatives)` is binding for the whole threshold — name it explicitly in the write-up, state why it was classified as a hard negative, and note that a different call on that one pair moves the threshold. For qualifications this is `Electrical Engineering`/`Electronic Engineering`: kept as a hard negative because they are distinct programmes with distinct competencies (power/heavy-current vs. circuits/signal processing/embedded), and a false positive here would credit a candidate with a qualification they do not hold — worse than a false negative a human reviewer can catch.

`Java` vs `JavaScript` is the skills litmus test. Lexically near-identical, semantically distinct. If the threshold cannot separate that pair, the engine is not defensible.

**Three calibration rounds were run; all three result sets are kept as evidence, not just the last one** (`test-fixtures/calibration-pairs/`):
- `calibration-results-v1-uncorrected.md` — the first run. True_match pairs for both domains were mostly abbreviations and full degree titles (`AWS`/`Amazon Web Services`, `BSc Computer Science`/`BEng Software Engineering`) rather than layer-3 content, and a labelling contamination existed (the same pair appeared as both true_match and hard_negative under different orderings). Result: 80% skills / 93.3% qualifications false-negative — a measurement of the wrong thing, not of the model.
- `calibration-results-v2-genre-mismatch.md` — pair sets corrected for contamination and rescoped to genuine layer-3 content, but skills true_match was written as multi-word descriptive phrases (2–5 words) against hard_negative/true_non_match's 1–2 word terms. Result: 100% skills false-negative. Diagnosis: a sentence embedder structurally scores longer, more complex text lower than near-identical short strings, regardless of true relatedness — the genre mismatch between categories, not the model's semantic judgement, was driving the number.
- `calibration-results.md` (current) — skills true_match rebuilt as PRIMARY (keyword vs keyword, genre-matched to hard_negative/true_non_match) plus SECONDARY (phrase vs keyword, informational). Result: **100% false-negative on skills PRIMARY, 0/15 secondary pairs clear the primary threshold, 100% false-negative on qualifications.** With both known construction errors corrected, this is the finding — see 5.4's "measured finding" and its stated consequence for the engine (normalisation + exact match does nearly all the real work; layer 3 is a rarely-firing fallback).

Deliverable: a calibration table with every pair, its measured similarity, and the derived threshold, for all three rounds. The before-and-after across rounds is the evidence that the sets were tested and corrected, not assumed correct on the first attempt.

#### 6.2 Ground-truth bands — derive before running

For each scored fixture, **before writing code**: hand-parse the CV, list the actual skills, qualifications and years, and compute the score on paper using the WS5 formulas.

Band = ground truth ± 5%. **The band never moves.** If a run lands outside it, diagnose rather than widen:

- Inspect the extraction JSON. Did the model miss entities? → extraction is failing, fix the prompt.
- Extraction correct but score wrong? → the scoring engine is failing, fix the JavaScript.

Widening a band until tests pass is the opposite of testing.

#### 6.3 Fixture set

Beyond the obvious matching / partial / mismatching CVs, include adversarial cases:

- A CV keyword-stuffed with the vacancy's exact requirement strings but no real experience behind them.
- A strong candidate using entirely different vocabulary from the vacancy.
- A career-changer with relevant skills under an unrelated job title.
- A non-computing degree holder with strong practical experience (tests the removed field whitelist).
- An academic CV over 15,000 characters (tests content-aware truncation).
- A CV with no standard section headings (tests the head-tail fallback).

Plus the failure fixtures:

| # | Input | Expected |
|---|---|---|
| F1 | Random non-CV document | `isCV: false`, no score at all, submission blocked |
| F2 | Blank document | Rejected at the Red band, zero Neurons spent |
| F3 | Image-only scanned PDF | Explicit extraction error, never a silent pass |
| F4 | Corrupt or password-protected file | Readable message |
| F5 | Worker URL pointed at a dead endpoint | Error state with retry — **not** a rejection message, **not** a pass |

F1 and F2 have **no numeric score**. A zero would rank a banana bread recipe below a weak engineer, implying it was evaluated as a candidate.

Keep every fixture in `/test-fixtures/` so results are reproducible live.

#### 6.4 Stability — two separate measurements

Report both, separately. They answer different questions.

**Scoring-stage variance: σ = 0.00.** Proven by a unit test feeding one static `CandidateProfile` object through the JavaScript engine 100 times. This is a genuine zero and it is provable.

**End-to-end variance: measured, not predicted.** Run the full pipeline 10 times per fixture with the AI Gateway cache **bypassed**, and report the actual standard deviation.

Do not claim end-to-end determinism. Extraction is an LLM call; even at temperature 0 with a fixed seed, serverless GPU inference is not bit-reproducible. Claiming σ = 0.00 end-to-end and having it fail when she runs it twice would be far worse than reporting a real number.

**A second, distinct source of end-to-end variance: embedding non-reproducibility.** Not the same thing as extraction variance above — this is the layer-3 equivalence-matching call (5.4), and it was found empirically, not anticipated. WS6.1 re-ran the qualifications calibration twice against the **identical** pair set (no code or data change between runs) and the derived threshold moved: 0.8701 → 0.8782. The pair that binds the threshold (`Electrical Engineering`/`Electronic Engineering`) shifted more than the best true-match pair, which flipped that pair from passing to failing between runs. Consequence for the live engine: CV skills are embedded fresh at scoring time (5.5), so a skill sitting near the threshold can score above it on one run and below it on the next — changing that skill's match status, the application's score, and potentially its rank — with no change to the CV, the vacancy, or the code. This is a real, separate contributor to end-to-end variance, distinct from LLM extraction variance, and it originates in a layer WS6.1 also measured at 100% false-negative (5.4). It is being measured, not assumed away — see the firing-rate and BORDERLINE instrumentation in `engine.js` and the open decision recorded in 5.4.

**Caching must be off for these runs.** A cached response proves the cache works, not that the model is stable. Presenting a cached result as proof of determinism would be dishonest, and she would be right to say so.

#### 6.5 Rank stability — the metric that actually matters

Score variance is mathematical noise. Rank instability breaks the product. A candidate moving 72 → 68 is unimportant; a candidate moving rank 12 → rank 2 destroys the shortlist's credibility.

Measure across the 10 repeated runs:
- **Position stability** — standard deviation of each candidate's rank.
- **Spearman's ρ** — overall monotonicity of the ordering.

Targets: ranks 1–3 identical across all runs (achievable because of the deterministic tie-break chain in 5.8), ranks 4–10 within ±1 position, ρ ≥ 0.85.

Report score variance and rank stability as two distinct results.

#### 6.6 Heuristic catch rate

Log every fixture's pre-filter routing (Red / Yellow / Green) during test runs.

```
Local catch rate = Red-zone rejections / total junk fixtures submitted
```

**Report whatever it measures.** If it comes out at 40%, that is the finding and the heuristics need work. Never write a target figure down in advance.

#### 6.7 Deliverable

A results document containing: the two calibration tables with derived thresholds, the ground-truth band table, the fixture results with observed score ranges, both stability measurements, the rank-stability figures, the heuristic catch rate, and the unverifiable-terms log showing what the model hallucinated and what caught it.

Automate what can be automated so it re-runs after every change. If a case fails, fix the system and re-run — **never adjust the expected result to match the output.**

**Role-based access tests.** Security is one of the six things she named as needing time per feature, and an unproven rule is not evidence:

| # | Logged in as | Attempt | Expected |
|---|---|---|---|
| R1 | Candidate | Open an HR pipeline or shortlist URL directly | Blocked |
| R2 | Candidate | Read another candidate's record or CV | Blocked |
| R3 | Candidate | Read their own match score | Blocked — internal to HR |
| R4 | HR | Full access to vacancies, applications, scores | Allowed |
| R5 | Signed out | Open any HR route directly by URL | Blocked, redirected |
| R6 | Any role | Read a `draft` or `closed` vacancy publicly | Blocked |

Test against **Firestore rules**, not the UI. Hiding a route in React is not access control — attempt the read from the console and confirm the rule rejects it.

---

## 9. WS7 (cut) and WS8 (reinstated)

### WS7 — Cross-vacancy live candidate board *(cut)*

**Do not build this.**

It was proposed as a cross-vacancy realtime board with activity trails and CSV export, justified by a classmate's recollection that Altrium's problem is hiring scattered across spreadsheets. That recollection is unverified, and the same person's account has already proved unreliable elsewhere. Under rule 4.8, a feature with no nameable stated problem behind it is out of scope.

The screen HR actually needs already exists inside **WS5**: for a given vacancy, applications ranked by match score with a threshold control. That is the screen that turns 100 CVs into a shortlist, and it is mandatory. It is not optional polish on WS5 — without it, the filtration runs in the database and the user sees nothing.

If Altrium's real brief turns out to state the spreadsheet/scattered-hiring problem explicitly, revisit this — but only after Tier 1 is complete and evidenced.

---

### WS8 — Interview scheduling and interviewer availability *(REINSTATED 10 Sep)*

**This was descoped. She reversed that in the 10 September meeting. It is required.**

> *"Okay, so before you do this, you need to fix the schedule interview feature, right?"* — immediately following *"Score a candidate."*

> *"But anyhow you need to be able to look into their availability."*

The earlier descoping decision is void. Do not put the descoping statement in the backlog.

---

#### 8.0 She offered a major simplification — take it

> *"So in order to make things more easier for you, convenient for you, you can scope down your solution and say that, let's say your solution is only considering DevOps interviews... So then you are only concerning about that domain."*

> *"Then in your backend, the possible interviewers only need to be from this DevOps specialization."*

**Scope the entire scheduling feature to one domain: DevOps.** This is not a shortcut we invented; she proposed it unprompted to make the feature completable. Taking it is the correct answer, and it is what makes end-to-end completion realistic in the time available.

Consequences:
- Interviewers in the system are DevOps specialists only.
- Vacancies that use scheduling are DevOps vacancies.
- State the scope limitation explicitly in the demo and the documentation, citing her suggestion.

---

#### 8.1 The four variables she named

> *"One is the specialization of the interviewer. Then the availability of the interviewer. If it is an in-person interview, the venue selection, right. And the stages of the interview."*

**1. Specialization.** Interviewers carry a specialization. Within DevOps, they are further categorised by the level they can interview:

> *"in that table you can categorize it, like if it is an intern interview, these three can go, likewise... If it is a senior DevOps engineer, these senior DevOps people who are working in [unclear] can go."*

So the eligible interviewer set is a function of (domain, level). An intern interview draws from one group; a senior DevOps interview draws from a different, smaller group.

**2. Availability.** See 8.2.

**3. Venue.** Only relevant for in-person interviews. Interviews carry a mode (`in_person` | `online`); in-person interviews require a venue selection, online ones do not. Keep this simple — a small set of seeded meeting rooms with double-booking prevention is enough.

**4. Stages, varying by seniority.**

> *"If it is an intern, maybe two interviews enough. HR interview and technical interview. If it is a senior DevOps engineer, maybe it can go up to even five or four stages."*

Stage count and composition are driven by the vacancy's level, not configured identically for every role. Intern: 2 stages. Junior: 3. Senior: 4–5. Make the mapping data-driven, not hardcoded per vacancy.

---

#### 8.2 Availability — the mechanism

Her wording, and the full extent of what she required:

> *"So these people, let's say eight people are there. They are currently working in projects in [unclear]... So they are not always free to do these five interviews. So HR person needs to have some kind of a way to check their availability."*

On the calendar question, a student asked directly whether they need a calendar API:

> Student: *"So it's like we have to get a calendar API for that."*
> Lecturer: *"You can. So you need to investigate what are the options you have."*

And earlier, on adding a calendar: *"That's one option. But anyhow you need to be able to look into their availability."*

**So a calendar API is permitted, not mandated.** What is mandated is that HR can check availability, and that we have **investigated the options** — she asked for that explicitly. Produce a short written comparison of the options considered (Google Calendar API, Microsoft Graph, declared availability in-app) with the reasoning for the choice. That investigation is itself a deliverable.

**Chosen mechanism: declared availability with expiry**, behind a provider interface so a real calendar can replace it.

Justification for the documentation: interviewers are Altrium employees whose work calendars live in Altrium's systems, which we cannot access. Declaring availability in-app is what real ATS products (Greenhouse, Lever) fall back to when calendar sync is not configured. The provider interface means a real calendar source can be substituted without changing any consuming code.

**The failure mode, and the rule that kills it.**

An interviewer declares themselves available, then forgets. Weeks later they are deep in a project, Hyre still shows them free, HR books them, the candidate's time is wasted. Stale availability is worse than none, because it produces confident wrong answers.

> **The system never infers `available` from silence.**

| State | Meaning | Schedulable |
|---|---|---|
| `available` | Declared available, within the validity window | Yes |
| `unavailable` | Declared unavailable, or on leave | No |
| `unknown` | Never declared, or the declaration has expired | **No** |

An interviewer who has never logged in is `unknown`. A lapsed declaration reverts to `unknown` — **never** to `available`. `unknown` is never suggested for a slot.

---

#### 8.2b Remove the existing "Available" pill first

`StageConfigModal.jsx` currently shows a green **"Available"** label for any interviewer with zero Hyre stage assignments. It measures how many pipeline stages the person is ticked onto — nothing about their real schedule. Someone slammed at their actual DevOps job who simply hasn't been assigned in Hyre reads as available.

That is exactly the failure 8.2 exists to prevent, using "no assignment records" as the silent stand-in for "free". It is not stored as truth, but it is **displayed** as truth to HR, which is what matters.

Remove it, or rename it to what it actually measures — "Assigned to N other stages". Do this before any real availability state is introduced, or HR will see two different things both claiming to say whether someone is free.

#### 8.3 Data model

```
interviewers/{userId}
  name
  domain: 'devops'                     // scoped per 8.0
  levels: ('intern' | 'junior' | 'senior')[]   // which levels they can interview

availability/{userId}
  slots: [ { dayOfWeek, startTime, endTime } ]      // recurring weekly
  exceptions: [ { date, type: 'leave' | 'blocked', reason } ]
  declaredAt: timestamp
  validUntil: timestamp                              // declaredAt + 14 days

interviews/{interviewId}
  applicationId, vacancyId, stageIndex
  interviewerId
  mode: 'in_person' | 'online'
  venueId | null
  scheduledAt
  status: 'pending_confirmation' | 'confirmed' | 'declined' | 'completed'
```

`state` is **computed at read time, never stored**:

```js
function availabilityState(record, now) {
  if (!record || !record.declaredAt) return 'unknown';
  if (now > record.validUntil)       return 'unknown';   // lapsed, not available
  if (record.onLeave)                return 'unavailable';
  return 'available';
}
```

Computing on read means a lapsed declaration cannot linger as stale truth because a background job failed.

**Validity window: 14 days.** Chosen deliberately — long enough not to nag interviewers weekly, short enough that a declaration cannot survive a change of project assignment. Record the reasoning; an arbitrary number invites the question.

---

#### 8.4 Confirm before commit

Expiry reduces staleness; it cannot eliminate it. So a booking is never final on a declaration alone:

1. HR selects a slot from the interviewer's declared availability.
2. The interview is created as `pending_confirmation`.
3. The interviewer is notified and must accept or decline.
4. Only on acceptance does it become `confirmed`.
5. A decline returns the slot to HR and marks the interviewer unavailable for that date.

An acceptance today is fresher evidence than a declaration from twelve days ago. This converts a stale-data problem into a confirmation step.

---

#### 8.5 Freshness must be visible

- Every interviewer shows when their availability was last updated. "Updated 2 hours ago" and "Updated 16 days ago" are different claims.
- Anything older than 7 days is visually flagged as ageing.
- `unknown` interviewers appear in the list but are clearly non-schedulable, with a "Request availability" action. Hiding them would make HR think the person does not exist.

---

#### 8.6 Selection: the system suggests, a human decides

Given the eligible pool — correct domain, correct level for the vacancy's seniority, `available` state, no venue clash — the system ranks suitable interviewers and recommends them.

**It must not assign automatically.** Removing the human from "who interviews this candidate" removes accountability from a hiring decision, and "the system chose" is not an answer that survives a viva question. Same principle as WS5: the engine reports, the human decides.

---

#### 8.7 End-to-end, or not at all

She was explicit about why Sprint 1 failed:

> *"During the demo all your features had gaps in them... without fine tuning and completing a function you have gone to other functions. So your product has many functions. But mainly they are partially completed."*

> *"Features needs to be end-to-end completed, all inclusive."*

For WS8 that means the whole chain works with no gaps: shortlisted candidate → stages determined by vacancy level → eligible interviewers filtered by domain, level and availability → slot selected → venue if in-person → interviewer confirms → interview appears for both parties → outcome recorded.

If any link cannot be finished, the feature is worth nothing. This is the second attempt at it.

---

#### 8.8 Stated limitation

Include in the documentation:

> *Declared availability with expiry and confirmation is a mitigation, not a complete solution. Fully accurate availability requires integration with Altrium's internal calendar system, which is outside the scope of this project. All availability reads go through an `AvailabilityProvider` interface so a real calendar source can replace declared availability without changing consuming code. Scheduling is scoped to the DevOps domain, as suggested by the Product Owner, so the feature could be completed end-to-end.*


---

## 10. Per-feature quality bar

She listed exactly what each feature needs time for. Every Tier 1 feature must satisfy all of it before moving on:

- **Validity** — inputs validated; invalid states unreachable.
- **Security** — Firestore rules restrict by role and are tested; a candidate cannot read the pipeline, other candidates, or scores; no keys in the client bundle; Storage rules deployed.
- **Accepts bad data** — empty, malformed, oversized, wrong-type, adversarial inputs all handled with readable messages.
- **Testing** — evidenced, especially WS6.
- **Design** — consistent, uses existing Tailwind/shadcn tokens, no new colour values.
- **User friendly** — a non-technical person can complete the task without instruction.

### 10.1 — AI failure states

Every requirement above assumes the Worker returns clean JSON. It will not always. Timeouts, rate limits, malformed output and outright refusals are normal operating conditions, not edge cases, and an application that hangs or fails silently at one of them is a defect a marker will find.

Define the behaviour for each failure, per stage:

| Stage | Failure | Required behaviour |
|---|---|---|
| Validation (WS3) | Worker unreachable or times out | Distinct error state with retry. **Never** a rejection message, **never** a silent pass. Submit stays disabled |
| Validation | Malformed JSON after one retry | Fall back to the heuristic verdict rather than blocking a valid candidate. Log that the fallback was used |
| Validation | Rate limited (429) | Back off and retry once, then the error state. Tell the user to try again shortly, not that their CV is invalid |
| Parsing (WS4) | Fails after the application was accepted | **The application still exists.** Status `failed`, CV preserved in Storage, surfaced to HR in a "needs manual review" queue with a Retry parse action. Never lose an applicant because our pipeline broke |
| Parsing | Returns partial or implausible data | Store what parsed, mark the missing fields as unextracted, flag for review. Do not fill gaps with guesses |
| Filtration (WS5) | Scoring fails for one application | That application shows "not scored — retry" in the list, not a zero. A zero is a judgement; a failure is not |
| Filtration | Vacancy has no structured requirements | Block scoring with a clear message telling HR to complete the vacancy |
| Any | Worker misconfigured | Health check on app load surfaces it immediately, rather than when a candidate applies |

Two rules that override any of the above:

- **A system failure must never be presented to a user as a judgement about them.** "We couldn't check your CV right now" and "this doesn't look like a CV" are different messages and must never be swapped.
- **A failure must never resolve in the permissive direction.** If validation cannot run, submission is blocked. Never allowed through.

Log every AI failure with stage, cause and status code. Include failure cases in the WS6 test evidence — deliberately point the client at a dead Worker URL and demonstrate the behaviour. She asked to see that the AI was tested, and testing how it fails is part of that.

---

## 11. Demo preparation *(carries marks)*

She called out unprepared demos directly: teams with no data, typing `ABCD EFGH IJK` live.

- **Demonstration and presentation together are 20 minutes.** She said *"you cannot think at time"* — it must be rehearsed.
- Prepare a **seeded demo dataset in the repo**, ready before the day:
  - Sample HR user and sample vacancies with fully populated requirements.
  - A set of realistic sample CVs: strong match, weak match, partial matches.
  - The failure fixtures from WS6: random document, blank document, scanned PDF.
  - Sample candidate registration data.
- Add a one-command seed script so the demo state is reproducible and never typed live.
- Rehearse the full chain end to end: post vacancy → find it publicly → apply → validation blocks a junk file → apply with a real CV → parsing → filtration → shortlist.
- Rehearse the **failure cases deliberately** — the junk-document rejection is the single strongest thing to show her, because it is the exact test she asked for.
- Divide the presentation between all four members in advance. She praised teams who did this and were ready when she walked in.

---

## 12. Agile process *(this is where Sprint 1 marks were actually lost)*

> *"Many teams have got lower marks than they thought is because they have not done the rest of it. Not the development."*

### 12.1 — She is running this as a real Agile project, and she is the Product Owner

She said both directly. She also said what happens if the team does not play along:

> *"My efforts in making this project into a realistic Agile project will fail if for you it is not a realistic Agile project... if you are engaging in the project just like you have engaged in all other projects so far."*

This is graded on process, not only output. Treat her as the client: she is the person the product is being built for, she is who requirements get clarified with, and she is who gets told when something changes.

### 12.2 — The waterfall tell

Her diagnosis of the failing pattern, in order:

1. Waste the first three weeks.
2. Start development without conceptualizing.
3. Do some development, then try to draw diagrams afterwards to match.
4. Try to combine it all at the end. *"And it doesn't work."*

Sprint 2 must run the other way round: conceptualize, review as a team, then build. If diagrams are being drawn after the code, the team is still in waterfall.

### 12.2b — Named in the 10 September meeting

Three specific corrections, all graded, none of them code:

- **The sprint goal is not a list of features.** *"So, sprint goal is not basically writing down the features, right? So, goal is when you develop those features, what can [unclear]."* The Sprint 2 goal states what the features achieve, not what they are.
- **Order the backlog.** *"Backlog, you need to organize, right, from first feature to last feature."*
- **The diagrams have notational errors.** *"Diagrams mostly have notational errors, right, so you need to look into them."* And when asked whether all three reviewed them together before submitting: *"You need to review together, right."* Reviewing together is not optional advice — she asked the question because the errors showed nobody had.

Also stated: the final report uses a **document template** containing the Sprint 1 document, the Sprint 2 document and further components. She said she would **put up the instructions by the following Tuesday** — check for them.

**Individual assignment (separate from this project):** a skill assessment / skill audit table with scores and justifications. Skills are drawn from the requirements stated in the **10 vacancies**. Skills may be categorised, and a category may carry one score and one justification rather than one per skill. She stated no maximum number.

### 12.3 — Stand-ups from week one

She noticed teams that only started daily stand-ups in week four or five, near the submission. Start them on day one of Sprint 2 and keep a record. The record is evidence.

### 12.4 — The backlog must be honest

- Poor backlog prioritisation and a backlog *"all over the place"* were named concerns.
- The sprint backlog must reflect what was actually developed. She checks this.
- She never mandated a feature count: *"Have I told anybody to develop 7 features, 6 features, 5 features, 4 features?"* Planning fewer items and completing them is the correct answer, not a compromise. She explicitly said a team that planned nine features, delivered three, and had an MVP **could not lose marks for it**: *"I cannot reduce mark if it is obvious problem solving product."*
- Scrum board: stories represent features and move through the lifecycle. She praised teams who understood this from week one.

### 12.5 — Conceptualization is a team activity, not the BA's homework

- Activity and sequence diagrams for the filtration flow get reviewed by all four members **before** it is built. Her point was that diagrams drawn in isolation from development is exactly how a team misses the variables inside a feature — she used interview scheduling as the example, with its interviewer type, capability, availability and seniority phases.
- **Diagrams must be updated when the product changes.** *"We changed the product at the last moment so we didn't have time to go back and change the diagrams"* is not an acceptable answer — it is something you raise with the Product Owner when it happens.
- **Do not submit a diagram nobody can explain.** She saw generated diagrams students could not defend when asked.

### 12.6 — Contribution is visible

- *"There are some students who have the understanding that development of the product is the only important factor."* It is not. Every team had a working product; the differentiator was everything around it.
- Every member reviews every other member's work. She said it is obvious at the sprint review which teams did this — one member knowing exactly what another is doing, and the whole team having sat down with each deliverable.
- Unequal workload was a named concern. Three of you, and a file full of items. Split it deliberately and record who did what.

### 12.7 — Retrospective, and using her

- The retrospective is done **with the Product Owner** — that is the industry practice she is modelling.
- Prepare answers: biggest achievement individually and as a team; biggest obstacle; whether the backlog reflected the development; how prepared the team would be if a requirement changed; one improvement guaranteed for Sprint 2.
- She offered Friday mornings for teams to come and see her, and stays after lectures for questions. Use it. Ambiguity in Altrium's brief is a question for the Product Owner, not something to guess at.

---

## 13. Verify before building

- **Get Altrium's actual brief and re-read it.** The stated problems currently in this file are second-hand. Her marking rule is that the product must address at least one problem *the scenario actually states*. Confirm the real list and confirm which one WS5 addresses.
- **Get the Sprint 2 deadline** from the correct submission link. No date was recoverable from the lecture recording. She said she will not look for submissions in the wrong link, and late is late.
- **WS8 is reinstated** as of the 10 Sep meeting. The descoping statement is void — do not put it in the backlog.

---

## 14. Working instructions for Claude Code

1. Fix the Worker (section 7) first. Report the real URL, status code and response body before proposing a fix.
2. Then run the WS1 audit and the section 5 mistake audit. Report findings. Write no feature code yet.
3. Then work Tier 1 in order: WS1 → WS2 verify → WS3 → WS4 → WS5 → WS6.
   Note: the WS6 threshold calibration (6.1) must run before WS5 can score anything. The engine throws until it does.
4. Before each workstream: state the plan and the files that will change. Wait for confirmation.
5. Small commits, one logical change each, in my name only.
6. Do not refactor unrelated code.
7. Do not build WS7. WS8 is back in scope as of 10 Sep — see section 9.
8. If a requirement here is ambiguous, technically wrong, or conflicts with the codebase, say so and stop rather than guessing.
9. Do not add features not listed in this file. An extra half-finished feature is worth less than nothing.
